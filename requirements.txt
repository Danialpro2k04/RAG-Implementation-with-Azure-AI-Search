# requirements.txt

# For loading environment variables from a .env file
python-dotenv

# OpenAI client library (v1.x or later)
openai>=1.0.0

# LangChain core and community packages
langchain>=0.1.0
langchain-community>=0.0.20
langchain-openai>=0.0.8
langchain-text-splitters>=0.0.1

# Required by LangChain's AzureSearch wrapper
azure-search-documents==11.4.0
azure-identity  # <-- ADD THIS LINE

# Required by OpenAIEmbeddings for token counting
tiktoken